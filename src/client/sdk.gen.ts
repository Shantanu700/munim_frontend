// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { DeleteCoreApiKeysData, DeleteCoreApiKeysErrors, DeleteCoreLoginData, DeleteCoreLoginErrors, DeleteIngestProductsData, DeleteIngestProductsErrors, DeletePolicyPoliciesData, DeletePolicyPoliciesErrors, DeletePolicyPoliciesResponses, GetAuditEntriesData, GetAuditEntriesErrors, GetAuditEntriesResponses, GetAuditEntryData, GetAuditEntryErrors, GetAuditEntryResponses, GetAuditExportData, GetAuditExportErrors, GetAuditExportResponses, GetAuditVerifyData, GetAuditVerifyErrors, GetAuditVerifyResponses, GetBuyerMandateData, GetBuyerMandateErrors, GetBuyerMandateResponses, GetBuyerMandatesData, GetBuyerMandatesErrors, GetBuyerMandatesResponses, GetBuyerOrderData, GetBuyerOrderErrors, GetBuyerOrderResponses, GetBuyerOrdersData, GetBuyerOrdersErrors, GetBuyerOrdersResponses, GetCoreApiKeysData, GetCoreApiKeysErrors, GetCoreApiKeysResponses, GetCoreDropdownData, GetCoreDropdownErrors, GetCoreDropdownResponses, GetCoreLoginData, GetCoreLoginErrors, GetCoreLoginResponses, GetCoreMerchantsCategoriesData, GetCoreMerchantsCategoriesErrors, GetCoreMerchantsCategoriesResponses, GetCoreMerchantsDropdownData, GetCoreMerchantsDropdownErrors, GetCoreMerchantsDropdownResponses, GetCoreRazorpayData, GetCoreRazorpayErrors, GetCoreRazorpayResponses, GetDashboardAgentsData, GetDashboardAgentsErrors, GetDashboardAgentsResponses, GetDashboardCatalogReadsData, GetDashboardCatalogReadsErrors, GetDashboardCatalogReadsResponses, GetDashboardData, GetDashboardErrors, GetDashboardPaymentsData, GetDashboardPaymentsErrors, GetDashboardPaymentsResponses, GetDashboardPolicyData, GetDashboardPolicyErrors, GetDashboardPolicyResponses, GetDashboardProductsData, GetDashboardProductsErrors, GetDashboardProductsResponses, GetDashboardProductsRowsData, GetDashboardProductsRowsErrors, GetDashboardProductsRowsResponses, GetDashboardResponses, GetDashboardRevenueData, GetDashboardRevenueErrors, GetDashboardRevenueResponses, GetIngestData, GetIngestErrors, GetIngestProductData, GetIngestProductErrors, GetIngestProductResponses, GetIngestProductsData, GetIngestProductsErrors, GetIngestProductsResponses, GetIngestResponses, GetIngestStreamData, GetIngestStreamErrors, GetIngestStreamResponses, GetOrderData, GetOrderErrors, GetOrderResponses, GetOrdersCountsData, GetOrdersCountsErrors, GetOrdersCountsResponses, GetOrdersData, GetOrdersErrors, GetOrdersExportData, GetOrdersExportErrors, GetOrdersExportResponses, GetOrdersResponses, GetOrdersTilesData, GetOrdersTilesErrors, GetOrdersTilesResponses, GetPaymentData, GetPaymentErrors, GetPaymentResponses, GetPaymentsData, GetPaymentsErrors, GetPaymentsResponses, GetPolicyData, GetPolicyErrors, GetPolicyHistoryData, GetPolicyHistoryErrors, GetPolicyHistoryResponses, GetPolicyOverviewData, GetPolicyOverviewErrors, GetPolicyOverviewResponses, GetPolicyPoliciesData, GetPolicyPoliciesErrors, GetPolicyPoliciesResponses, GetPolicyResponses, GetPolicyRuleCatalogData, GetPolicyRuleCatalogErrors, GetPolicyRuleCatalogResponses, GetPolicyRuleData, GetPolicyRuleErrors, GetPolicyRuleResponses, GetPolicyStepUpData, GetPolicyStepUpErrors, GetPolicyStepUpResponses, GetWebhooksHitsData, GetWebhooksHitsErrors, GetWebhooksHitsResponses, PostBuyerMandatesData, PostBuyerMandatesErrors, PostBuyerMandatesResponses, PostBuyerMandatesRevokeData, PostBuyerMandatesRevokeErrors, PostBuyerMandatesRevokeResponses, PostCoreApiKeysData, PostCoreApiKeysErrors, PostCoreApiKeysResponses, PostCoreGoogleData, PostCoreGoogleErrors, PostCoreGoogleResponses, PostCoreLoginData, PostCoreLoginErrors, PostCoreLoginResponses, PostCoreRazorpayTestEventData, PostCoreRazorpayTestEventErrors, PostCoreRazorpayTestEventResponses, PostCoreRazorpayVerifyData, PostCoreRazorpayVerifyErrors, PostCoreRazorpayVerifyResponses, PostCoreRazorpayWebhookSecretData, PostCoreRazorpayWebhookSecretErrors, PostCoreRazorpayWebhookSecretResponses, PostCoreRegisterData, PostCoreRegisterErrors, PostCoreRegisterResponses, PostIngestCancelData, PostIngestCancelErrors, PostIngestCancelResponses, PostIngestData, PostIngestErrors, PostIngestProductsData, PostIngestProductsErrors, PostIngestProductsResponses, PostIngestResponses, PostPolicyPoliciesActivateData, PostPolicyPoliciesActivateErrors, PostPolicyPoliciesActivateResponses, PostPolicyPoliciesData, PostPolicyPoliciesDeactivateData, PostPolicyPoliciesDeactivateErrors, PostPolicyPoliciesDeactivateResponses, PostPolicyPoliciesErrors, PostPolicyPoliciesResponses, PostPolicyStepUpData, PostPolicyStepUpErrors, PostPolicyStepUpResponses, PostWebhooksRazorpay2Data, PostWebhooksRazorpay2Errors, PostWebhooksRazorpay2Responses, PostWebhooksRazorpayData, PostWebhooksRazorpayErrors, PostWebhooksRazorpayResponses, PutCoreRazorpayData, PutCoreRazorpayErrors, PutCoreRazorpayResponses, PutIngestProductsData, PutIngestProductsErrors, PutIngestProductsResponses, PutPolicyKillSwitchData, PutPolicyKillSwitchErrors, PutPolicyKillSwitchResponses, PutPolicyPoliciesData, PutPolicyPoliciesErrors, PutPolicyPoliciesResponses, PutPolicyPoliciesRulesData, PutPolicyPoliciesRulesErrors, PutPolicyPoliciesRulesResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * The audit table: every decision this store made, newest first.
 *
 * Paginated on `seq` descending rather than on a timestamp. `seq` is unique
 * per tenant and gapless, so a page boundary cannot duplicate or skip a row
 * the way two entries sharing a timestamp can -- and it is the same order the
 * chain itself is built in.
 */
export const getAuditEntries = <ThrowOnError extends boolean = false>(options?: Options<GetAuditEntriesData, ThrowOnError>): RequestResult<GetAuditEntriesResponses, GetAuditEntriesErrors, ThrowOnError> => (options?.client ?? client).get<GetAuditEntriesResponses, GetAuditEntriesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/audit/entries',
    ...options
});

/**
 * One entry, with the hash it carries and the one it commits to.
 *
 * Addressed by `seq` -- the number the table already shows, and the number a
 * broken verify names.
 */
export const getAuditEntry = <ThrowOnError extends boolean = false>(options: Options<GetAuditEntryData, ThrowOnError>): RequestResult<GetAuditEntryResponses, GetAuditEntryErrors, ThrowOnError> => (options.client ?? client).get<GetAuditEntryResponses, GetAuditEntryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/audit/entries/{seq}',
    ...options
});

/**
 * The whole chain, in the form someone else can re-hash.
 *
 * Not paginated and not filtered, deliberately: a partial chain cannot be
 * verified. Every entry from genesis is needed to recompute the hashes, so
 * this returns them all, in chain order, as the columns that were hashed.
 */
export const getAuditExport = <ThrowOnError extends boolean = false>(options?: Options<GetAuditExportData, ThrowOnError>): RequestResult<GetAuditExportResponses, GetAuditExportErrors, ThrowOnError> => (options?.client ?? client).get<GetAuditExportResponses, GetAuditExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/audit/export',
    ...options
});

/**
 * Public proof endpoint. Verifies the hash chain for one or every tenant.
 */
export const getAuditVerify = <ThrowOnError extends boolean = false>(options?: Options<GetAuditVerifyData, ThrowOnError>): RequestResult<GetAuditVerifyResponses, GetAuditVerifyErrors, ThrowOnError> => (options?.client ?? client).get<GetAuditVerifyResponses, GetAuditVerifyErrors, ThrowOnError>({ url: '/audit/verify', ...options });

/**
 * The caller's mandates, and new ones.
 */
export const getBuyerMandates = <ThrowOnError extends boolean = false>(options?: Options<GetBuyerMandatesData, ThrowOnError>): RequestResult<GetBuyerMandatesResponses, GetBuyerMandatesErrors, ThrowOnError> => (options?.client ?? client).get<GetBuyerMandatesResponses, GetBuyerMandatesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/buyer/mandates/',
    ...options
});

/**
 * The caller's mandates, and new ones.
 */
export const postBuyerMandates = <ThrowOnError extends boolean = false>(options: Options<PostBuyerMandatesData, ThrowOnError>): RequestResult<PostBuyerMandatesResponses, PostBuyerMandatesErrors, ThrowOnError> => (options.client ?? client).post<PostBuyerMandatesResponses, PostBuyerMandatesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/buyer/mandates/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resolve the caller's Buyer row and scope every query to it.
 */
export const getBuyerMandate = <ThrowOnError extends boolean = false>(options: Options<GetBuyerMandateData, ThrowOnError>): RequestResult<GetBuyerMandateResponses, GetBuyerMandateErrors, ThrowOnError> => (options.client ?? client).get<GetBuyerMandateResponses, GetBuyerMandateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/buyer/mandates/{mandate_uuid}/',
    ...options
});

/**
 * Resolve the caller's Buyer row and scope every query to it.
 */
export const postBuyerMandatesRevoke = <ThrowOnError extends boolean = false>(options: Options<PostBuyerMandatesRevokeData, ThrowOnError>): RequestResult<PostBuyerMandatesRevokeResponses, PostBuyerMandatesRevokeErrors, ThrowOnError> => (options.client ?? client).post<PostBuyerMandatesRevokeResponses, PostBuyerMandatesRevokeErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/buyer/mandates/{mandate_uuid}/revoke/',
    ...options
});

/**
 * Every order this buyer has placed, newest first.
 *
 * Across all merchants, unlike the MCP tools, which are pinned to the store
 * whose endpoint the agent is talking to. A logged-in buyer asking for their
 * own orders is not scoped to one shop.
 *
 * Paginated by cursor and not by page number, for the reason OrderListAV
 * states: the list grows at the head, so an order placed between two scrolls
 * shifts every offset, and the reader sees one row twice and never sees
 * another. The ordering KeysetPagination positions on -- (-authorized_at,
 * -id) -- is this model's Meta ordering and is covered here by
 * idx_order_buyer_authz, so the buyer's page costs the same index walk the
 * merchant's does.
 *
 * `total` comes back only on a request that arrived without a cursor: that is
 * the one that needs "Showing 20 of 147", and counting again on every scroll
 * tick buys nothing.
 */
export const getBuyerOrders = <ThrowOnError extends boolean = false>(options?: Options<GetBuyerOrdersData, ThrowOnError>): RequestResult<GetBuyerOrdersResponses, GetBuyerOrdersErrors, ThrowOnError> => (options?.client ?? client).get<GetBuyerOrdersResponses, GetBuyerOrdersErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/buyer/orders/',
    ...options
});

/**
 * One order, with every payment attempt made against it.
 */
export const getBuyerOrder = <ThrowOnError extends boolean = false>(options: Options<GetBuyerOrderData, ThrowOnError>): RequestResult<GetBuyerOrderResponses, GetBuyerOrderErrors, ThrowOnError> => (options.client ?? client).get<GetBuyerOrderResponses, GetBuyerOrderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/buyer/orders/{order_uuid}/',
    ...options
});

/**
 * Issue, list, and revoke platform API keys for the calling user.
 *
 * Session-authenticated (BaseAV's default authentication = True): only a
 * logged-in buyer/user can mint a key for themselves, to use with the
 * platform-wide MCP endpoint. The raw key is returned once, on creation --
 * only its hash is ever stored, so it cannot be shown again after this.
 */
export const getCoreApiKeys = <ThrowOnError extends boolean = false>(options?: Options<GetCoreApiKeysData, ThrowOnError>): RequestResult<GetCoreApiKeysResponses, GetCoreApiKeysErrors, ThrowOnError> => (options?.client ?? client).get<GetCoreApiKeysResponses, GetCoreApiKeysErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/api-keys/',
    ...options
});

/**
 * Issue, list, and revoke platform API keys for the calling user.
 *
 * Session-authenticated (BaseAV's default authentication = True): only a
 * logged-in buyer/user can mint a key for themselves, to use with the
 * platform-wide MCP endpoint. The raw key is returned once, on creation --
 * only its hash is ever stored, so it cannot be shown again after this.
 */
export const postCoreApiKeys = <ThrowOnError extends boolean = false>(options?: Options<PostCoreApiKeysData, ThrowOnError>): RequestResult<PostCoreApiKeysResponses, PostCoreApiKeysErrors, ThrowOnError> => (options?.client ?? client).post<PostCoreApiKeysResponses, PostCoreApiKeysErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/api-keys/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

export const deleteCoreApiKeys = <ThrowOnError extends boolean = false>(options: Options<DeleteCoreApiKeysData, ThrowOnError>): RequestResult<unknown, DeleteCoreApiKeysErrors, ThrowOnError> => (options.client ?? client).delete<unknown, DeleteCoreApiKeysErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/api-keys/{key_uuid}',
    ...options
});

export const getCoreDropdown = <ThrowOnError extends boolean = false>(options: Options<GetCoreDropdownData, ThrowOnError>): RequestResult<GetCoreDropdownResponses, GetCoreDropdownErrors, ThrowOnError> => (options.client ?? client).get<GetCoreDropdownResponses, GetCoreDropdownErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/dropdown/{parent_slug}/',
    ...options
});

/**
 * Google Login/Sign Up API View
 */
export const postCoreGoogle = <ThrowOnError extends boolean = false>(options: Options<PostCoreGoogleData, ThrowOnError>): RequestResult<PostCoreGoogleResponses, PostCoreGoogleErrors, ThrowOnError> => (options.client ?? client).post<PostCoreGoogleResponses, PostCoreGoogleErrors, ThrowOnError>({
    url: '/core/google/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Login/Logout API View
 */
export const deleteCoreLogin = <ThrowOnError extends boolean = false>(options?: Options<DeleteCoreLoginData, ThrowOnError>): RequestResult<unknown, DeleteCoreLoginErrors, ThrowOnError> => (options?.client ?? client).delete<unknown, DeleteCoreLoginErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/login/',
    ...options
});

/**
 * Login/Logout API View
 */
export const getCoreLogin = <ThrowOnError extends boolean = false>(options?: Options<GetCoreLoginData, ThrowOnError>): RequestResult<GetCoreLoginResponses, GetCoreLoginErrors, ThrowOnError> => (options?.client ?? client).get<GetCoreLoginResponses, GetCoreLoginErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/login/',
    ...options
});

/**
 * Login/Logout API View
 */
export const postCoreLogin = <ThrowOnError extends boolean = false>(options: Options<PostCoreLoginData, ThrowOnError>): RequestResult<PostCoreLoginResponses, PostCoreLoginErrors, ThrowOnError> => (options.client ?? client).post<PostCoreLoginResponses, PostCoreLoginErrors, ThrowOnError>({ url: '/core/login/', ...options });

/**
 * The categories of one store, addressed by the merchant's UUID.
 *
 * Unauthenticated, deliberately: this is what an agent or a storefront reads
 * to learn what a store sells, before there is any session to authenticate.
 * Nothing here is private -- the names are the same ones the merchant's own
 * catalogue pages already publish.
 *
 * A store that exists but has no categories yet is an empty list rather than
 * a 404, matching MasterDropdownAV: "not crawled yet" is a legitimate answer,
 * and only a UUID naming no store is a wrong request.
 */
export const getCoreMerchantsCategories = <ThrowOnError extends boolean = false>(options: Options<GetCoreMerchantsCategoriesData, ThrowOnError>): RequestResult<GetCoreMerchantsCategoriesResponses, GetCoreMerchantsCategoriesErrors, ThrowOnError> => (options.client ?? client).get<GetCoreMerchantsCategoriesResponses, GetCoreMerchantsCategoriesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/merchants/{merchant_uuid}/categories/',
    ...options
});

/**
 * Every onboarded store, as a dropdown reads it: uuid, domain, name.
 *
 * Authenticated (BaseAV's default): MerchantCategoryListAV is open because a
 * caller who already knows a store's UUID learns nothing new from its
 * catalogue, but this route is the thing that hands out those UUIDs, and the
 * roster of who is on the platform is not public.
 *
 * A store with no domain yet is left out. It has no MCP or webhook URL, so it
 * cannot be transacted with, and its label would render as a blank row.
 */
export const getCoreMerchantsDropdown = <ThrowOnError extends boolean = false>(options?: Options<GetCoreMerchantsDropdownData, ThrowOnError>): RequestResult<GetCoreMerchantsDropdownResponses, GetCoreMerchantsDropdownErrors, ThrowOnError> => (options?.client ?? client).get<GetCoreMerchantsDropdownResponses, GetCoreMerchantsDropdownErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/merchants/dropdown/',
    ...options
});

/**
 * The store's Razorpay linkage: read the whole setup panel, or save credentials.
 *
 * GET returns the entire screen in one call, the way IngestAV.get does for the
 * catalogue step -- including whether a webhook has ever arrived, so the "waiting
 * for the first webhook" line resolves by re-polling this rather than needing a
 * route of its own. The two halves of the screen describe one moment; fetched
 * separately they can disagree.
 */
export const getCoreRazorpay = <ThrowOnError extends boolean = false>(options?: Options<GetCoreRazorpayData, ThrowOnError>): RequestResult<GetCoreRazorpayResponses, GetCoreRazorpayErrors, ThrowOnError> => (options?.client ?? client).get<GetCoreRazorpayResponses, GetCoreRazorpayErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/razorpay/',
    ...options
});

/**
 * Partial, matching the house convention: correcting the key id must not
 * require re-typing a secret Razorpay will never show again.
 */
export const putCoreRazorpay = <ThrowOnError extends boolean = false>(options?: Options<PutCoreRazorpayData, ThrowOnError>): RequestResult<PutCoreRazorpayResponses, PutCoreRazorpayErrors, ThrowOnError> => (options?.client ?? client).put<PutCoreRazorpayResponses, PutCoreRazorpayErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/razorpay/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Send a signed, synthetic event to the store's own webhook URL.
 *
 * Munim cannot ask Razorpay to send anything, so it sends what Razorpay would,
 * signed the way Razorpay would sign it, over the public internet. That is what
 * makes it worth having: it exercises DNS, TLS, the proxy, the route and the
 * stored secret together, which is the actual list of things broken when a
 * merchant reports that webhooks never arrive.
 */
export const postCoreRazorpayTestEvent = <ThrowOnError extends boolean = false>(options?: Options<PostCoreRazorpayTestEventData, ThrowOnError>): RequestResult<PostCoreRazorpayTestEventResponses, PostCoreRazorpayTestEventErrors, ThrowOnError> => (options?.client ?? client).post<PostCoreRazorpayTestEventResponses, PostCoreRazorpayTestEventErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/razorpay/test-event/',
    ...options
});

/**
 * Prove the stored key pair actually authenticates against Razorpay.
 *
 * Accepts an inline pair so the UI can check before committing. A rejected key is
 * 200 with ok:false, not a 4xx -- the request was well formed and was answered;
 * the credentials are what failed, and that is a fact about the payload rather
 * than an error in the call.
 */
export const postCoreRazorpayVerify = <ThrowOnError extends boolean = false>(options?: Options<PostCoreRazorpayVerifyData, ThrowOnError>): RequestResult<PostCoreRazorpayVerifyResponses, PostCoreRazorpayVerifyErrors, ThrowOnError> => (options?.client ?? client).post<PostCoreRazorpayVerifyResponses, PostCoreRazorpayVerifyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/razorpay/verify/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Mint the webhook signing secret.
 *
 * Razorpay does not issue this one -- the merchant invents it when creating the
 * webhook -- so generating it here is what makes both sides match without asking
 * anyone to copy a string in two directions.
 */
export const postCoreRazorpayWebhookSecret = <ThrowOnError extends boolean = false>(options?: Options<PostCoreRazorpayWebhookSecretData, ThrowOnError>): RequestResult<PostCoreRazorpayWebhookSecretResponses, PostCoreRazorpayWebhookSecretErrors, ThrowOnError> => (options?.client ?? client).post<PostCoreRazorpayWebhookSecretResponses, PostCoreRazorpayWebhookSecretErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/core/razorpay/webhook-secret/',
    ...options
});

/**
 * Sign Up API View
 */
export const postCoreRegister = <ThrowOnError extends boolean = false>(options: Options<PostCoreRegisterData, ThrowOnError>): RequestResult<PostCoreRegisterResponses, PostCoreRegisterErrors, ThrowOnError> => (options.client ?? client).post<PostCoreRegisterResponses, PostCoreRegisterErrors, ThrowOnError>({
    url: '/core/register/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The whole screen in one call.
 *
 * One request rather than six, for the reason PolicyOverviewAV states: the
 * cards all describe the same moment, and assembled from separate calls the
 * screen can render a payments total that disagrees with the bars beneath it.
 *
 * `now` is taken ONCE and threaded through every function, so no two cards can
 * land on opposite sides of midnight. On a dashboard whose windows are
 * calendar days that is a real outcome at 00:00 local, not a theoretical one.
 */
export const getDashboard = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardData, ThrowOnError>): RequestResult<GetDashboardResponses, GetDashboardErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardResponses, GetDashboardErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/',
    ...options
});

/**
 * Which AI placed the orders, by revenue and count.
 *
 * The same payload as /dashboard/revenue, from the same aggregate, because the
 * bars and the line are one set of facts and the two cards are two readings of
 * it. Kept as its own route so the per-agent card can be refreshed on its own
 * window without the client having to know it shares an endpoint with the
 * chart. Every `agent` key is a valid ?agent= value on GET /orders/, which is
 * what makes a bar clickable.
 *
 * What this cannot tell a merchant is how agent baskets compare to their
 * storefront ones: create_order has exactly one caller, so every order in
 * this system is an agent order and there is no storefront denominator.
 * `prior_window` is the comparison that does exist.
 */
export const getDashboardAgents = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardAgentsData, ThrowOnError>): RequestResult<GetDashboardAgentsResponses, GetDashboardAgentsErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardAgentsResponses, GetDashboardAgentsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/agents',
    ...options
});

/**
 * How often agents read this store's catalogue, by day, agent and tool.
 */
export const getDashboardCatalogReads = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardCatalogReadsData, ThrowOnError>): RequestResult<GetDashboardCatalogReadsResponses, GetDashboardCatalogReadsErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardCatalogReadsResponses, GetDashboardCatalogReadsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/catalog-reads',
    ...options
});

/**
 * Money received, all time, with the month either side for context.
 *
 * The settlement block is the honest version of the design's
 * "Settled straight to acc_QK3n...4471": that Razorpay account id is
 * permanently unavailable, so this returns the masked key id and a sentence
 * composed server-side. See dashboard_analytics.settlement.
 */
export const getDashboardPayments = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardPaymentsData, ThrowOnError>): RequestResult<GetDashboardPaymentsResponses, GetDashboardPaymentsErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardPaymentsResponses, GetDashboardPaymentsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/payments',
    ...options
});

/**
 * The store-policy panel on its own.
 *
 * A thin read for a client that wants to refresh that panel without the whole
 * screen. The panel's "open" arrow goes to GET /policy/overview/, which
 * returns every policy, every rule and the policy tiles -- this deliberately
 * duplicates none of that, and is not the place to add an editor.
 */
export const getDashboardPolicy = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardPolicyData, ThrowOnError>): RequestResult<GetDashboardPolicyResponses, GetDashboardPolicyErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardPolicyResponses, GetDashboardPolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/policy',
    ...options
});

/**
 * Sellable vs total, and what the gap is waiting on.
 *
 * Holds each row against the details the store CURRENTLY enforces, so the
 * "Ready to sell" figure means "would sell today" rather than "satisfies a
 * rule that is still a draft". See _required.
 */
export const getDashboardProducts = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardProductsData, ThrowOnError>): RequestResult<GetDashboardProductsResponses, GetDashboardProductsErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardProductsResponses, GetDashboardProductsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/products',
    ...options
});

/**
 * The rows behind the number: agent-visible products missing one detail.
 *
 * The arrow on "4 waiting on one detail each" exists because the merchant
 * wants those four products, not the number four. Its own route rather than a
 * ?missing= mode on the card above, so each operation has one response shape
 * the schema can describe honestly -- a single path answering either counts or
 * a paginated row list documents as neither.
 *
 * Filters over every KNOWN detail, not only the required ones: a row can be
 * missing a detail the current policy does not ask for, and a merchant fixing
 * their catalogue wants to see that before they tighten the rule. Built from
 * dashboard_analytics.visible_products and the same PRESENT clause the card
 * counts with, so the list and the number cannot disagree.
 *
 * ?missing=any is the same list without having to name the detail first: every
 * visible row missing at least one known detail, each row once however many it
 * is missing. That is the whole "52 - 48" gap when the store requires all six,
 * and a superset of it when it requires fewer -- the same reason the
 * single-detail lists filter over every known field rather than the required
 * ones. It excludes with ALL_PRESENT, the conjunction of the very clauses the
 * per-detail lists exclude with, so `any` cannot list a row that no
 * single-detail list would.
 */
export const getDashboardProductsRows = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardProductsRowsData, ThrowOnError>): RequestResult<GetDashboardProductsRowsResponses, GetDashboardProductsRowsErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardProductsRowsResponses, GetDashboardProductsRowsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/products/rows',
    ...options
});

/**
 * The daily revenue series, plus the window before it to compare against.
 *
 * Exactly `days` points, zero-filled and in order, so the chart cannot skip a
 * date and the client never has to guess which day a gap belongs to.
 */
export const getDashboardRevenue = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardRevenueData, ThrowOnError>): RequestResult<GetDashboardRevenueResponses, GetDashboardRevenueErrors, ThrowOnError> => (options?.client ?? client).get<GetDashboardRevenueResponses, GetDashboardRevenueErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/dashboard/revenue',
    ...options
});

/**
 * Everything the catalogue screen needs on load, in one request.
 *
 * Deliberately not a bare product list: a client that has just reloaded also
 * needs to know whether an ingestion is still running, so it can re-attach its
 * EventSource instead of showing a stale, half-filled table with no explanation.
 */
export const getIngest = <ThrowOnError extends boolean = false>(options?: Options<GetIngestData, ThrowOnError>): RequestResult<GetIngestResponses, GetIngestErrors, ThrowOnError> => (options?.client ?? client).get<GetIngestResponses, GetIngestErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/',
    ...options
});

/**
 * Start an asynchronous catalog ingestion for the caller's merchant.
 */
export const postIngest = <ThrowOnError extends boolean = false>(options: Options<PostIngestData, ThrowOnError>): RequestResult<PostIngestResponses, PostIngestErrors, ThrowOnError> => (options.client ?? client).post<PostIngestResponses, PostIngestErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stop a running ingestion. The job settles on `cancelled`, its open SSE stream closes with a final `done` event, and the products already ingested stay in the catalogue. Safe to call twice, and safe to call on a job that has just finished -- `cancelled` in the response says whether this call is what stopped it.
 */
export const postIngestCancel = <ThrowOnError extends boolean = false>(options: Options<PostIngestCancelData, ThrowOnError>): RequestResult<PostIngestCancelResponses, PostIngestCancelErrors, ThrowOnError> => (options.client ?? client).post<PostIngestCancelResponses, PostIngestCancelErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/{job_uuid}/cancel',
    ...options
});

/**
 * Server-Sent Events stream of live progress for one ingestion job, open until the job finishes. Requires `EventSource(url, {withCredentials: true})` so the session cookie is sent.
 *
 * Named events:
 * - `progress` -- `{job, status, processed, total, created, updated, skipped}`, sent immediately on connect and again whenever a counter changes.
 * - `product` -- `{uuid, sku, title, price_paise, currency, image_url}`, sent whenever a product is newly ingested. Same shape as a row of `results` from `GET /ingest/products`, so a client renders either without telling them apart; `uuid` addresses the row for `GET /ingest/products/<uuid>` and its edit/delete.
 * - `done` -- final `progress`-shaped payload, sent once and the stream closes. Its `status` is `succeeded`, or `cancelled` if the merchant stopped the run via `POST /ingest/<job>/cancel`.
 * - `error` -- `{message}`, sent once (job failed) and the stream closes.
 */
export const getIngestStream = <ThrowOnError extends boolean = false>(options: Options<GetIngestStreamData, ThrowOnError>): RequestResult<GetIngestStreamResponses, GetIngestStreamErrors, ThrowOnError> => (options.client ?? client).get<GetIngestStreamResponses, GetIngestStreamErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/{job_uuid}/stream',
    ...options
});

/**
 * The caller's catalog, a page at a time, and manual additions to it.
 *
 * Paginated rather than served whole with the ingest state: a catalogue is
 * unbounded, and a merchant reloading the screen should not pay for every row
 * they own.
 */
export const getIngestProducts = <ThrowOnError extends boolean = false>(options?: Options<GetIngestProductsData, ThrowOnError>): RequestResult<GetIngestProductsResponses, GetIngestProductsErrors, ThrowOnError> => (options?.client ?? client).get<GetIngestProductsResponses, GetIngestProductsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/products',
    ...options
});

/**
 * The caller's catalog, a page at a time, and manual additions to it.
 *
 * Paginated rather than served whole with the ingest state: a catalogue is
 * unbounded, and a merchant reloading the screen should not pay for every row
 * they own.
 */
export const postIngestProducts = <ThrowOnError extends boolean = false>(options: Options<PostIngestProductsData, ThrowOnError>): RequestResult<PostIngestProductsResponses, PostIngestProductsErrors, ThrowOnError> => (options.client ?? client).post<PostIngestProductsResponses, PostIngestProductsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/products',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read, edit or remove one product in the caller's own catalog.
 */
export const deleteIngestProducts = <ThrowOnError extends boolean = false>(options: Options<DeleteIngestProductsData, ThrowOnError>): RequestResult<unknown, DeleteIngestProductsErrors, ThrowOnError> => (options.client ?? client).delete<unknown, DeleteIngestProductsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/products/{product_uuid}',
    ...options
});

/**
 * Everything about one product -- the columns the list omits so that a
 * catalogue page stays cheap.
 */
export const getIngestProduct = <ThrowOnError extends boolean = false>(options: Options<GetIngestProductData, ThrowOnError>): RequestResult<GetIngestProductResponses, GetIngestProductErrors, ThrowOnError> => (options.client ?? client).get<GetIngestProductResponses, GetIngestProductErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/products/{product_uuid}',
    ...options
});

/**
 * Partial on purpose: flipping is_visible_to_agents must not require the
 * client to echo back every other column to avoid clobbering it.
 */
export const putIngestProducts = <ThrowOnError extends boolean = false>(options: Options<PutIngestProductsData, ThrowOnError>): RequestResult<PutIngestProductsResponses, PutIngestProductsErrors, ThrowOnError> => (options.client ?? client).put<PutIngestProductsResponses, PutIngestProductsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/ingest/products/{product_uuid}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every order an agent placed, newest first.
 *
 * Paginated by cursor rather than page number: the list grows at the head, so
 * an order placed between two scrolls would shift every offset and the reader
 * would see one row twice and never see another. See base.pagination.
 *
 * `total` is returned only on a request that arrived without a cursor -- that
 * is the one that needs "Showing 140 of 147", and counting again on every
 * scroll tick buys nothing.
 */
export const getOrders = <ThrowOnError extends boolean = false>(options?: Options<GetOrdersData, ThrowOnError>): RequestResult<GetOrdersResponses, GetOrdersErrors, ThrowOnError> => (options?.client ?? client).get<GetOrdersResponses, GetOrdersErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/orders/',
    ...options
});

/**
 * One order, in full, with a link to the decision that allowed it.
 *
 * Addressed by uuid rather than order_number: order numbers are per-tenant
 * and sequential, so they are enumerable, and a 404-on-someone-else's-order
 * is worth less when the address itself can be guessed.
 */
export const getOrder = <ThrowOnError extends boolean = false>(options: Options<GetOrderData, ThrowOnError>): RequestResult<GetOrderResponses, GetOrderErrors, ThrowOnError> => (options.client ?? client).get<GetOrderResponses, GetOrderErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/orders/{order_uuid}',
    ...options
});

/**
 * The tab bar's numbers.
 *
 * Ignores ?status= -- it IS the status breakdown -- but honours ?q= and
 * ?agent=, because tab counts that ignored an active search would contradict
 * the list sitting underneath them. That asymmetry is the one thing here a
 * reader is likely to assume wrong, hence spelling it out.
 */
export const getOrdersCounts = <ThrowOnError extends boolean = false>(options?: Options<GetOrdersCountsData, ThrowOnError>): RequestResult<GetOrdersCountsResponses, GetOrdersCountsErrors, ThrowOnError> => (options?.client ?? client).get<GetOrdersCountsResponses, GetOrdersCountsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/orders/counts',
    ...options
});

/**
 * The current filter's rows, as JSON.
 *
 * Departs from LedgerExportAV in both the ways that matter, and on purpose.
 * That one refuses to filter and refuses to cap because a partial chain
 * cannot be re-hashed. Neither holds here: this is a report, not a proof, so
 * it honours the filters -- the button says "Export these 140" -- and it
 * stops at EXPORT_LIMIT with `truncated` set and the filters echoed back, so
 * a merchant who hits the ceiling can see what to narrow.
 */
export const getOrdersExport = <ThrowOnError extends boolean = false>(options?: Options<GetOrdersExportData, ThrowOnError>): RequestResult<GetOrdersExportResponses, GetOrdersExportErrors, ThrowOnError> => (options?.client ?? client).get<GetOrdersExportResponses, GetOrdersExportErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/orders/export',
    ...options
});

/**
 * The three KPI tiles: paid this month, awaiting approval, average order.
 *
 * Ignores every filter, deliberately -- a KPI is a fact about the store, not
 * about whichever tab happens to be open. `LedgerExportAV` states its own
 * version of this for the same reason.
 *
 * "Awaiting approval" counts step-ups waiting on a human, which is a
 * different table from orders waiting on a card. The orders list has no such
 * status; see the `source` field on GET /orders/counts.
 */
export const getOrdersTiles = <ThrowOnError extends boolean = false>(options?: Options<GetOrdersTilesData, ThrowOnError>): RequestResult<GetOrdersTilesResponses, GetOrdersTilesErrors, ThrowOnError> => (options?.client ?? client).get<GetOrdersTilesResponses, GetOrdersTilesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/orders/tiles',
    ...options
});

/**
 * Every payment link this store has minted, newest first.
 *
 * Across orders, and one row per ATTEMPT -- which is the difference from
 * /orders/, where three tries collapse into one row showing whichever attempt
 * reproject() promoted.
 *
 * Paginated by cursor rather than page number for the reason OrderListAV
 * states: the list grows at the head, so a retry minted between two scrolls
 * shifts every offset and the reader sees one row twice and never sees
 * another. The (-requested_at, -id) walk is covered by
 * idx_payment_tenant_req_id.
 *
 * `total` comes back only on a request that arrived without a cursor -- that
 * is the one that needs "Showing 20 of 147", and counting again on every
 * scroll tick buys nothing.
 */
export const getPayments = <ThrowOnError extends boolean = false>(options?: Options<GetPaymentsData, ThrowOnError>): RequestResult<GetPaymentsResponses, GetPaymentsErrors, ThrowOnError> => (options?.client ?? client).get<GetPaymentsResponses, GetPaymentsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/payments/',
    ...options
});

/**
 * One attempt, with a link to the decision that authorised it.
 *
 * Addressed by uuid rather than (order_number, attempt): order numbers are
 * per-tenant and sequential, so that pair is enumerable, and a 404-on-someone-
 * else's-row is worth less when the address itself can be guessed.
 */
export const getPayment = <ThrowOnError extends boolean = false>(options: Options<GetPaymentData, ThrowOnError>): RequestResult<GetPaymentResponses, GetPaymentErrors, ThrowOnError> => (options.client ?? client).get<GetPaymentResponses, GetPaymentErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/payments/{payment_uuid}',
    ...options
});

/**
 * Every switch and edit, newest first, with who made it and when.
 *
 * Read from the money chain rather than a separate table, so a change is as
 * tamper-evident as the decisions around it -- and so the order of the two
 * relative to each other is a fact rather than a guess from timestamps.
 */
export const getPolicyHistory = <ThrowOnError extends boolean = false>(options?: Options<GetPolicyHistoryData, ThrowOnError>): RequestResult<GetPolicyHistoryResponses, GetPolicyHistoryErrors, ThrowOnError> => (options?.client ?? client).get<GetPolicyHistoryResponses, GetPolicyHistoryErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/history/',
    ...options
});

/**
 * Agent traffic on or off, store-wide.
 *
 * Deliberately not a rule and not on a policy: it has to be reachable when
 * the store has no active policy, which is exactly when a merchant most
 * wants it. It overrides every rule rather than sitting among them.
 */
export const putPolicyKillSwitch = <ThrowOnError extends boolean = false>(options: Options<PutPolicyKillSwitchData, ThrowOnError>): RequestResult<PutPolicyKillSwitchResponses, PutPolicyKillSwitchErrors, ThrowOnError> => (options.client ?? client).put<PutPolicyKillSwitchResponses, PutPolicyKillSwitchErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/kill-switch/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The whole screen in one call.
 *
 * One request rather than five, because the tiles, the filter chips and the
 * table all describe the same moment: assembled from separate calls, the
 * screen can render a total that disagrees with the rows beneath it.
 */
export const getPolicyOverview = <ThrowOnError extends boolean = false>(options?: Options<GetPolicyOverviewData, ThrowOnError>): RequestResult<GetPolicyOverviewResponses, GetPolicyOverviewErrors, ThrowOnError> => (options?.client ?? client).get<GetPolicyOverviewResponses, GetPolicyOverviewErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/overview/',
    ...options
});

/**
 * The caller's policies, and new ones.
 *
 * Not paginated, unlike the catalogue: a merchant keeps a handful of these,
 * and the screen shows all of them with counts.
 */
export const getPolicyPolicies = <ThrowOnError extends boolean = false>(options?: Options<GetPolicyPoliciesData, ThrowOnError>): RequestResult<GetPolicyPoliciesResponses, GetPolicyPoliciesErrors, ThrowOnError> => (options?.client ?? client).get<GetPolicyPoliciesResponses, GetPolicyPoliciesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/',
    ...options
});

/**
 * The caller's policies, and new ones.
 *
 * Not paginated, unlike the catalogue: a merchant keeps a handful of these,
 * and the screen shows all of them with counts.
 */
export const postPolicyPolicies = <ThrowOnError extends boolean = false>(options: Options<PostPolicyPoliciesData, ThrowOnError>): RequestResult<PostPolicyPoliciesResponses, PostPolicyPoliciesErrors, ThrowOnError> => (options.client ?? client).post<PostPolicyPoliciesResponses, PostPolicyPoliciesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One policy, its rules, and what each of them stopped.
 */
export const deletePolicyPolicies = <ThrowOnError extends boolean = false>(options: Options<DeletePolicyPoliciesData, ThrowOnError>): RequestResult<DeletePolicyPoliciesResponses, DeletePolicyPoliciesErrors, ThrowOnError> => (options.client ?? client).delete<DeletePolicyPoliciesResponses, DeletePolicyPoliciesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/{policy_uuid}/',
    ...options
});

/**
 * One policy, its rules, and what each of them stopped.
 */
export const getPolicy = <ThrowOnError extends boolean = false>(options: Options<GetPolicyData, ThrowOnError>): RequestResult<GetPolicyResponses, GetPolicyErrors, ThrowOnError> => (options.client ?? client).get<GetPolicyResponses, GetPolicyErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/{policy_uuid}/',
    ...options
});

/**
 * One policy, its rules, and what each of them stopped.
 */
export const putPolicyPolicies = <ThrowOnError extends boolean = false>(options: Options<PutPolicyPoliciesData, ThrowOnError>): RequestResult<PutPolicyPoliciesResponses, PutPolicyPoliciesErrors, ThrowOnError> => (options.client ?? client).put<PutPolicyPoliciesResponses, PutPolicyPoliciesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/{policy_uuid}/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Put one policy in force, or leave the store with none in force.
 */
export const postPolicyPoliciesActivate = <ThrowOnError extends boolean = false>(options: Options<PostPolicyPoliciesActivateData, ThrowOnError>): RequestResult<PostPolicyPoliciesActivateResponses, PostPolicyPoliciesActivateErrors, ThrowOnError> => (options.client ?? client).post<PostPolicyPoliciesActivateResponses, PostPolicyPoliciesActivateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/{policy_uuid}/activate/',
    ...options
});

/**
 * Switch the active policy off without deleting it.
 *
 * A real state, not an absence of one: no active policy means no store rules
 * are applied at all, which is what a merchant is choosing here.
 */
export const postPolicyPoliciesDeactivate = <ThrowOnError extends boolean = false>(options: Options<PostPolicyPoliciesDeactivateData, ThrowOnError>): RequestResult<PostPolicyPoliciesDeactivateResponses, PostPolicyPoliciesDeactivateErrors, ThrowOnError> => (options.client ?? client).post<PostPolicyPoliciesDeactivateResponses, PostPolicyPoliciesDeactivateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/{policy_uuid}/deactivate/',
    ...options
});

/**
 * Configure one rule: its parameters, whether it enforces, and what
 * happens when it is breached.
 *
 * Addressed by `kind` rather than by uuid. The kinds are a fixed backend
 * registry, so the slug is a stable address the client already knows from the
 * catalogue -- and it is what lets a rule row be seeded on read without the
 * client having to re-fetch to learn its id.
 */
export const getPolicyRule = <ThrowOnError extends boolean = false>(options: Options<GetPolicyRuleData, ThrowOnError>): RequestResult<GetPolicyRuleResponses, GetPolicyRuleErrors, ThrowOnError> => (options.client ?? client).get<GetPolicyRuleResponses, GetPolicyRuleErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/{policy_uuid}/rules/{kind}/',
    ...options
});

/**
 * Partial on purpose: flipping the toggle must not require the client
 * to echo back the whole config to avoid clobbering it.
 */
export const putPolicyPoliciesRules = <ThrowOnError extends boolean = false>(options: Options<PutPolicyPoliciesRulesData, ThrowOnError>): RequestResult<PutPolicyPoliciesRulesResponses, PutPolicyPoliciesRulesErrors, ThrowOnError> => (options.client ?? client).put<PutPolicyPoliciesRulesResponses, PutPolicyPoliciesRulesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/policies/{policy_uuid}/rules/{kind}/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The rule kinds this backend knows how to enforce.
 *
 * The editor renders its form from this rather than hardcoding a panel per
 * kind, so a rule added to the registry needs no frontend release. The kinds
 * are fixed here and not creatable: each one needs a checker and a place in
 * the engine's sequence, neither of which a form can supply.
 *
 * Resolved against the caller's store even though the registry is global:
 * the category rules' options are that store's own categories, so a catalogue
 * served without a tenant would offer a picker with nothing in it.
 */
export const getPolicyRuleCatalog = <ThrowOnError extends boolean = false>(options?: Options<GetPolicyRuleCatalogData, ThrowOnError>): RequestResult<GetPolicyRuleCatalogResponses, GetPolicyRuleCatalogErrors, ThrowOnError> => (options?.client ?? client).get<GetPolicyRuleCatalogResponses, GetPolicyRuleCatalogErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/policy/rule-catalog/',
    ...options
});

/**
 * The human approval surface. A control surface, not a product surface.
 *
 * The token in the URL is the capability -- there is no login.
 */
export const getPolicyStepUp = <ThrowOnError extends boolean = false>(options: Options<GetPolicyStepUpData, ThrowOnError>): RequestResult<GetPolicyStepUpResponses, GetPolicyStepUpErrors, ThrowOnError> => (options.client ?? client).get<GetPolicyStepUpResponses, GetPolicyStepUpErrors, ThrowOnError>({ url: '/policy/step-up/{token}/', ...options });

/**
 * The human approval surface. A control surface, not a product surface.
 *
 * The token in the URL is the capability -- there is no login.
 */
export const postPolicyStepUp = <ThrowOnError extends boolean = false>(options: Options<PostPolicyStepUpData, ThrowOnError>): RequestResult<PostPolicyStepUpResponses, PostPolicyStepUpErrors, ThrowOnError> => (options.client ?? client).post<PostPolicyStepUpResponses, PostPolicyStepUpErrors, ThrowOnError>({
    url: '/policy/step-up/{token}/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The caller's Razorpay webhook hit log, most recent first.
 */
export const getWebhooksHits = <ThrowOnError extends boolean = false>(options?: Options<GetWebhooksHitsData, ThrowOnError>): RequestResult<GetWebhooksHitsResponses, GetWebhooksHitsErrors, ThrowOnError> => (options?.client ?? client).get<GetWebhooksHitsResponses, GetWebhooksHitsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/webhooks/hits',
    ...options
});

/**
 * Receive a Razorpay webhook.
 *
 * /webhooks/razorpay/<merchant> is the real route: the tenant comes from the
 * path, so we know WHOSE secret to verify with before trusting anything. A
 * single global secret cannot work for two merchants, because you would have
 * to decide who sent a payload in order to check whether it is genuine.
 *
 * /webhooks/razorpay (no merchant) is deprecated and kept only so an already
 * configured dashboard URL does not silently stop working mid-migration.
 */
export const postWebhooksRazorpay = <ThrowOnError extends boolean = false>(options?: Options<PostWebhooksRazorpayData, ThrowOnError>): RequestResult<PostWebhooksRazorpayResponses, PostWebhooksRazorpayErrors, ThrowOnError> => (options?.client ?? client).post<PostWebhooksRazorpayResponses, PostWebhooksRazorpayErrors, ThrowOnError>({ url: '/webhooks/razorpay', ...options });

/**
 * Receive a Razorpay webhook.
 *
 * /webhooks/razorpay/<merchant> is the real route: the tenant comes from the
 * path, so we know WHOSE secret to verify with before trusting anything. A
 * single global secret cannot work for two merchants, because you would have
 * to decide who sent a payload in order to check whether it is genuine.
 *
 * /webhooks/razorpay (no merchant) is deprecated and kept only so an already
 * configured dashboard URL does not silently stop working mid-migration.
 */
export const postWebhooksRazorpay2 = <ThrowOnError extends boolean = false>(options: Options<PostWebhooksRazorpay2Data, ThrowOnError>): RequestResult<PostWebhooksRazorpay2Responses, PostWebhooksRazorpay2Errors, ThrowOnError> => (options.client ?? client).post<PostWebhooksRazorpay2Responses, PostWebhooksRazorpay2Errors, ThrowOnError>({ url: '/webhooks/razorpay/{merchant}', ...options });
